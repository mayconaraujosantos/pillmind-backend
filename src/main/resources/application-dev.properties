# Ambiente de desenvolvimento
APP_ENV=dev

# Database (DEV) — SQLite (arquivo no diretório de trabalho ao rodar o app)
DATABASE_URL=jdbc:sqlite:pillmind.dev.db
DATABASE_USER=
DATABASE_PASSWORD=

# MinIO (opcional) — docker compose -f docker-compose.minio.yml up -d
# MINIO_ENABLED=true
# MINIO_ENDPOINT=http://127.0.0.1:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=pillmind
# URL que o app mobile usa para carregar imagens (emulador Android: http://10.0.2.2:9000/pillmind)
# MINIO_PUBLIC_BASE_URL=http://127.0.0.1:9000/pillmind
